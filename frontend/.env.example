# Public site config
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Google Analytics 4 (Paket A §4, UAT-13)
NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX
# Sentry (ITIL Observability)
NEXT_PUBLIC_SENTRY_DSN=https://examplePublicKey@o0.ingest.sentry.io/0

# WhatsApp CTA (Paket A §3)
# Accepts +62..., 62..., or local 08... (will be normalized to digits for wa.me)
NEXT_PUBLIC_WHATSAPP_NUMBER=6281511688745
# NOTE: Provide plain text. It will be URL-encoded by the app.
NEXT_PUBLIC_WHATSAPP_PREFILL=Hi Alfa Beauty, saya ingin konsultasi produk.

# Fallback contact (Paket A §3)
NEXT_PUBLIC_FALLBACK_EMAIL=alfabeautycosmeticaa@gmail.com

# --- Supabase (Lead Persistence) ---
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ==============================================================================
# SECURITY TOKENS (OWASP A07 / ISO 27001 A.9)
# ==============================================================================

# --- Admin Token (for Export CSV) ---
# SECURITY: Required for admin endpoints. Minimum 32 characters.
# ROTATION: Change this value monthly and redeploy.
# COBIT: DSS05.04 - Ensure access tokens are managed and revocable.
LEAD_API_ADMIN_TOKEN=your-32-character-secure-admin-token

# --- Health Check Token (for Deep Monitoring) ---
# SECURITY: Enables /api/health?deep=true endpoint. Minimum 16 characters.
# Without this token, deep health check returns 403.
# Usage: curl -H "Authorization: Bearer $HEALTH_CHECK_TOKEN" /api/health?deep=true
HEALTH_CHECK_TOKEN=your-16-char-ops-token

# ==============================================================================
# DISTRIBUTED RATE LIMITING (OWASP API4:2023)
# ==============================================================================

# Optional: Enable Upstash Redis for distributed rate limiting
# Required for multi-instance/serverless deployments to prevent bypass
# Get credentials at: https://console.upstash.com/
UPSTASH_REDIS_REST_URL=https://your-redis.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-upstash-token

# ==============================================================================
# EMAIL NOTIFICATION
# ==============================================================================

# --- Email Notification (Optional) ---
SMTP_ENABLED=false
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=alfabeautycosmeticaa@gmail.com
SMTP_PASS=
SMTP_FROM=alfabeautycosmeticaa@gmail.com
SMTP_TO=alfabeautycosmeticaa@gmail.com
