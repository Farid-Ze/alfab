# Documentation Index (Moved)

This repository’s documentation has been split into two packages:

- **Paket A (Website):** `../docs-paket-a/paket-a.md`
- **Paket B (B2B Digital Hub / Platform):** `../docs-paket-b/`

This `docs/` folder is intentionally kept as a **shim** for legacy links.
Please update any old references from `docs/...` to the correct package folder.
